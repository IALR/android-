<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Bluetooth and WiFi -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="33" />

    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />

    <application
        android:name=".RobotControlApp"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RobotControl">
        
        <!-- Splash Activity as launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        
        <!-- Pairing Activity -->
        <activity
            android:name=".PairingActivity"
            android:exported="false" />
        
        <!-- Control Activity -->
        <activity
            android:name=".ControlActivity"
            android:exported="false" />
        
        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- App Settings Activity -->
        <activity
            android:name=".AppSettingsActivity"
            android:exported="false"
            android:label="@string/app_settings"
            android:parentActivityName=".MainActivity" />
        
        <!-- Main Dashboard Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Robot List Activity -->
        <activity
            android:name=".RobotListActivity"
            android:exported="false"
            android:label="@string/my_robots" />
        
        <!-- Robot Control Activity -->
        <activity
            android:name=".RobotControlActivity"
            android:exported="false"
            android:label="@string/robot_wifi_control_label"
            android:parentActivityName=".MainActivity" />

        <!-- Education Home Activity -->
        <activity
            android:name=".EducationHomeActivity"
            android:exported="false"
            android:label="@string/education_hub_title"
            android:parentActivityName=".MainActivity" />
        
        <!-- Education Activity -->
        <activity
            android:name=".EducationActivity"
            android:exported="false"
            android:label="@string/education_activity_label"
            android:parentActivityName=".EducationHomeActivity" />

        <!-- Quiz Activity -->
        <activity
            android:name=".QuizActivity"
            android:exported="false"
            android:label="@string/education_tab_quiz"
            android:parentActivityName=".EducationHomeActivity" />

        <!-- Robotics Game Activity -->
        <activity
            android:name=".RoboticsGameActivity"
            android:exported="false"
            android:label="@string/robotics_game_title"
            android:parentActivityName=".EducationHomeActivity" />

        <!-- Circuit Builder Activity -->
        <activity
            android:name=".CircuitBuilderActivity"
            android:exported="false"
            android:label="@string/circuit_builder_title"
            android:parentActivityName=".EducationHomeActivity" />
        
        <!-- Auto Reconnect Service -->
        <service
            android:name=".services.AutoReconnectService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>